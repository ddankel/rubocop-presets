source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rubocop-presets.gemspec
gemspec

group :development do
  gem "bundler", "~> 2.0"
  gem "rake", "~> 13.0"
  gem "solargraph"
end
